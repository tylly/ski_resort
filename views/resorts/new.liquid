{% layout 'layout.liquid' %} {% block content %}

<div id="searchResortMain">
  <form id="logInForm" action="/resorts/new" method="POST">
    <fieldset>
      <legend>Search Resort</legend>
      <div id="resortSearch">
        <input
          type="text"
          name="resortId"
          placeholder="Resort"
          class="form-control"
        />
      </div>
    </fieldset>
    <div class="userEnterr">
      <input
        type="submit"
        value="Search"
        class="btn btn-success"
        id="logInBut"
      />
    </div>
  </form>
</div>

{% endblock %}
